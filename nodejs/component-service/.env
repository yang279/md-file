DASHSCOPE_API_KEY=sk-01fc3058d8fd453d8a19731b99d3aff7

# 多组件库根目录（hex 静态资源所在路径）。不配置时默认指向 ../../pixso-parse/pix-split/lib-out，
# 仅当部署环境需要把这份数据放在别处时才需要取消下面这行的注释并改成实际路径——
# 写在配置文件里就不会在每次启动时忘记带上它
#LIB_OUT_DIR=/path/to/lib-out
