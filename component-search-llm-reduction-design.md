# 组件搜索 LLM 调用次数优化 — 方案设计与对比

> 背景：生产实现在 [nodejs/component-service/match_variant.js](nodejs/component-service/match_variant.js)（[pixso-parse/pix-split/enrich/](pixso-parse/pix-split/enrich/) 下是旧版/开发副本，仅供历史对照，不代表当前线上逻辑）。
>
> **现状（已核实生产代码）**：
> - **单条查询 `matchVariant`**（[match_variant.js:423-459](nodejs/component-service/match_variant.js#L423-L459)）确实是 **3 次** LLM 调用：
>   `normalizeQuery`(语义提取, L86) → `selectComponentSet`(选组件集, L139) → `selectVariant`(精选变体, L180)
>   拆成 3 次的原因写在注释里（[L133-137](nodejs/component-service/match_variant.js#L133-L137)）：10 个候选组件集 × 上百变体，若把全部变体名一次性塞进 prompt 会膨胀到上万 token，所以先用轻量 prompt 选出 1 个组件集，再只展开它自己的变体精选——这是一次主动的"调用次数 ↔ prompt 体积"权衡，而非疏漏。
> - **整页批量路径 `matchVariantsTogether`**（[L380-419](nodejs/component-service/match_variant.js#L380-L419)，被 [match_dsl.js](nodejs/component-service/match_dsl.js) 调用）已经把 `3×N` 压到了 `1 + 去重后的组件集组数 G`：
>   1. **直接跳过了语义提取**（[L390](nodejs/component-service/match_variant.js#L390)）——本地过滤直接用 `buildQuery` 拼好的原始 query，不调 `normalizeQuery`
>   2. **所有实例的候选池去重合并后一次性选组件集**（[selectComponentSetsTogether, L237-299](nodejs/component-service/match_variant.js#L237-L299)）
>   3. **按选中的组件集分组，每组只跑一次组内选变体**（[selectVariantsTogether, L301-358](nodejs/component-service/match_variant.js#L301-L358)）
>
> 这意味着我们讨论的两条核心优化思路——**"规则替代语义提取"**和**"批量化解耦调用次数与实例数"**——已经在 `matchVariantsTogether` 里落地并验证可行，不是纸上谈兵。本方案在此基础上讨论如何把这些经验反哺到尚是 3 次调用的单条查询路径，以及更大规模（多库）场景下的延伸优化。

## 新方案：整体处理流程（输入 → 匹配 → 输出）

把前面讨论的修改点整合进去后，两条路径的端到端流程分别长这样（**带 [新增] 标记的是相对当前生产代码的改动点**，其余步骤维持原状）：

### A. matchVariant（单条匹配）新流程

```
输入 description
   │
   ├─ [新增] 缓存查询：description（或归一化后的 key）是否命中结果缓存？
   │     └─ 命中 → 直接返回缓存结果（0 次 LLM）
   │
   ├─ [新增] 条件判断：description 是否已经"够中文/够结构化"
   │   （中文字符占比、是否已含组件类别词等规则判断，阈值需离线数据验证）
   │     ├─ 是 → 跳过语义提取，直接拿 description 去本地过滤
   │     └─ 否 → 调 normalizeQuery（语义提取，第 1 次 LLM）→ 得到中文关键词
   │
   ├─ localFilter 本地过滤（无 LLM，瞬间完成）
   │     用（关键词 或 原始 description）在索引里打分 → Top-K 候选组件集
   │     [可选，依赖精度验证] K 从 10 收紧到 3
   │
   └─ 选组件集 + 选变体
         ├─ [若 K 收紧到 3 且验证可行] 合并成一次调用：
         │     直接在 3 个候选集的完整变体列表中选出最终变体（第 2 次 LLM，也是最后一次）
         └─ [否则维持现状两步]
               selectComponentSet（选组件集，第 2 次 LLM，轻量 prompt 不展开变体）
               → selectVariant（精选变体，第 3 次 LLM，只展开选中集合的变体）
   │
   └─ [新增] 写入结果缓存，返回结果
```

**调用次数变化**：缓存命中 0 次；命中"可跳过语义提取"分支且合并选集/选变体成功时低至 2 次；其余情况维持现状 3 次（不会比现在更差）。

### B. matchVariantsTogether（整页批量匹配）新流程 —— 含一致性安全网

> ⚠️ **重要修正**：最初设计的"逐条查缓存 + 逐条高置信度直通"是有问题的——会破坏 `matchVariantsTogether` 最核心的一致性保证（详见下方"为什么需要安全网"）。下面是修正后的版本：把"摘出去独立处理"改成"先聚类、再整簇决策、决策结果回灌给 LLM 做对齐基准"。

```
输入 queries[]（从 DSL 节点 label + semantic 通过 buildQuery 规则拼接而成，已跳过语义提取——现状如此）
   │
   ├─ [新增] Step 0：按文本归一化做聚类分组
   │     完全相同 / 高度相似的 query 聚成一簇（例如 5 个"确定按钮"聚一簇）
   │     —— 一簇只解析一次，结果广播给簇内所有实例，天然保证簇内一致
   │
   ├─ [新增] Step 1：查"规范映射表"（canonical mapping，按簇查，不按条查）
   │     这是一张项目/设计系统级别的字典：{ 簇的归一化 key → 已确认的 (组件集, 变体) }
   │     由历史 LLM 裁决结果沉淀而来，不是临时缓存
   │     ├─ 整簇命中 → 直接采用映射表结果，簇内/跨页天然一致，摘出待处理列表
   │     └─ 未命中 → 保留在待处理列表
   │
   ├─ localFilter 本地过滤，对每一簇的代表 query 打分（无 LLM，现状逻辑不变）→ Top-K 候选
   │
   ├─ [新增] Step 2：整簇高置信度判断（判定粒度是"簇"，不是"条"）
   │     整簇的 Top-1 是否显著高于 Top-2，且簇内所有成员打分结果一致？
   │     ├─ 是 → 整簇直通，仍然要在最终汇总前"对齐"——见下方安全网第 2 条
   │     └─ 否 / 簇内有分歧 → 整簇归入 matchable 集合，交给 LLM 集中裁决
   │
   ├─ matchable 集合的候选池去重合并 → 一次性调用 selectComponentSetsTogether
   │     [新增] prompt 里追加"已锁定的对齐基准"：把 Step 1/2 已确定的簇结果作为
   │     已知信息传入（"以下元素已确定使用 XX 组件集 YY 变体，请让相近元素与之对齐"）
   │     （选组件集，第 1 次 LLM —— 现状如此，新增内容只是 prompt 信息量，不增加调用次数）
   │
   ├─ 按选中的组件集分组，每组调用一次 selectVariantsTogether（同样附带对齐基准）
   │     （精选变体，每个不同组件集分组各 1 次 —— 现状如此，组数记为 G）
   │
   └─ [新增] 汇总 (映射表命中簇 + 直通簇 + LLM 裁决簇) 按原始顺序展开回每个实例
         把本次新确认的簇结果合并写回"规范映射表"（而不是写临时缓存），返回
```

**调用次数变化**：现状是 `1 + G`；叠加"聚类 + 规范映射表命中 + 整簇直通"后，真正需要 LLM 裁决的是**簇**而不是条，且映射表会随着使用不断成熟、命中率持续上升——理想情况下可以降到 `0 ~ (1 + G')`（`G' ≤ G` 且通常 `G' ≪ G`，因为很多簇会被映射表直接吃掉）；最坏情况（全是新簇、映射表为空）退化为现状 `1 + G`，不会变差。

---

### 为什么需要"一致性安全网"，以及它具体是什么

`matchVariantsTogether` 的一致性保证，根源是**"所有实例一次性塞给同一次 LLM 调用，模型能彼此看见"**（[match_variant.js:223-227](nodejs/component-service/match_variant.js#L223-227)）——一致性是"集中裁决"这个机制天然带来的副产品，不是额外加的逻辑。

而"缓存命中"和"高置信度直通"这类优化，本质都是**在集中裁决之前，把一部分实例摘出去单独处理**。如果摘得不小心：同一页面 5 个"确定按钮"，3 个命中缓存（用的是历史结果 A），2 个没命中走本次 LLM 裁决（可能选出结果 B）——同一页面里出现 A、B 两种结果，一致性当场被破坏。这是任何"部分摘出独立处理"类优化的通病，必须专门补一道安全网，包含三层：

1. **先聚类、按簇决策，不按条决策**——同一簇（文本归一化后相同/高度相似的 label）只解析一次、结果广播给全簇，从根上保证"同一种元素必然得到同一个结果"，这本身也是减少工作量的优化（去重）
2. **已确认结果要"回灌"给 LLM 当对齐基准**——簇 A 已经命中映射表确定为"组件集 X 变体 Y"，在裁决其余簇时把这个事实写进 prompt（"以下元素已确定使用 X/Y，请让语义相近的元素与之对齐"），让模型在为剩余簇做决策时，把已锁定的选择当基准而不是另起炉灶
3. **直通的判定粒度必须是"簇"而不是"条"**——只有当整簇的本地过滤结果一致且置信度都高时才整簇直通；簇内任何成员打分有分歧或置信度不够，整簇退回集中裁决，避免"同一簇里有的走捷径有的没走"的撕裂

---

## -1. 一个值得马上做的低风险优化：把 matchVariantsTogether 的经验搬回 matchVariant

`matchVariantsTogether` 已经验证：用一张 `semantic → 中文提示` 的静态映射表（`SEMANTIC_HINT`，见 [match_dsl.js:11-20](nodejs/component-service/match_dsl.js#L11-L20)，如 `button → 按钮`）把节点的 `label` 拼接成 query（`buildQuery`，[match_dsl.js:40-47](nodejs/component-service/match_dsl.js#L40-L47)），就足够让本地过滤命中正确的候选组件集——**完全不需要 `normalizeQuery` 这次 LLM 调用**。

这给单条查询路径一个现成、低风险的优化方向：如果调用方能提供结构化的语义类型（`semantic`/`label` 这类信息，而不仅是一句自然语言 `description`），单条接口同样可以复用 `SEMANTIC_HINT` 做规则预处理，跳过第一步的语义提取，直接把 `matchVariant` 从 3 次降到 2 次。改动量小（复用现成映射表 + 跳过一次调用），且批量路径已经替它做过线上验证，风险可控。

## 0. 两个查找问题的本质区别

在设计优化方案前先厘清一点：**组件集查找**和**组件变体查找**是两类不同性质的问题，适合的技术也不同。

| | 组件集查找 | 组件变体查找 |
|---|---|---|
| 问题本质 | "这是个什么类型的组件"——语义/类别检索 | "这个类型里具体哪个配置"——结构化属性比对 |
| 输入特征 | 自然语言意图描述 | 变体名几乎都是 `状态=primary, 尺寸=large` 这种 key=value 组合 |
| 适合的技术 | embedding / 关键词检索 / 库级路由 | 属性名/属性值归一化表 + 精确/模糊字段比对 |
| 与语义理解的关系 | 强相关 | 弱相关，更依赖归一化做得好不好 |

现有 [build_index.js](pixso-parse/pix-split/enrich/build_index.js#L12-L20) 把组件集名称和变体名混在一起拼成 `searchText` 做检索打分，是个实用的权宜之计（能借变体名关键词提升召回），但概念上把两种不同粒度的信号搅在了一起。重新设计时值得把它们拆开处理。

## 1. 候选方案

### 方案 1：规则优先闸门（最小改动）

在现有 pipeline 前面加一道"规则匹配"关卡：先用属性归一化表对 `variant_props` 做精确/模糊字段比对，命中直接返回；未命中才落回现有的"语义提取 → 本地过滤 → LLM 精选"流程。

```
输入
 ├─ 有 variant_props？ → 归一化 + 字段比对 → 命中 → 直接返回（0 次 LLM）
 └─ 未命中 / 无结构化属性 → 走原有流程（最多 2 次 LLM，不变）
```

**重要澄清**：这不是"去 LLM 化"方案，而是"规则优先、LLM 兜底"的分流策略。它的实际收益完全取决于查询里有多少比例自带结构化 `variant_props`：
- 批量页面匹配场景（从 node-dsl 解析出的 instance 通常带 `variant_props`，如 `{"大小":"L","状态":"Normal"}`）→ 收益明显
- 单条 `/match` 接口的输入主要是自然语言描述（"主按钮大号"）→ 规则匹配基本帮不上忙，仍走完整 2 次 LLM 流程

### 方案 2：Embedding 检索替换关键词检索

把"语义提取（LLM）+ 本地关键词过滤"两步合并成一步"向量检索"：用 embedding 把用户描述和组件集索引都编码成向量，直接算相似度取 Top K，省掉语义提取这次 LLM 调用。

```
输入 → embedding 检索 Top K → LLM 精选变体（仍保留 1 次）
```

对自然语言输入同样有效，是对"主流量"更实际的优化方向。需要离线把现有索引（几百到几千条）预先编码成向量并缓存，运行时只算一次查询向量 + 余弦相似度。

### 方案 3：规则 + Embedding + 分层路由（综合方案）

方案 1 + 方案 2 叠加，再加上"库路由"（先按上下文/元数据缩小到具体库）和"重排"（粗召回 Top 50 → 轻量重排 Top 10）。这是为多库、大规模场景准备的目标架构。

```
输入 → 库路由(规则/检索) → 组件集检索(embedding) → 重排
     → 规则字段比对(命中→返回) / LLM 精选(未命中→兜底)
```

**多库场景下的额外考量**：
- 几千到一两万条 entry，内存里算余弦相似度仍然够快（<100ms），不需要引入向量数据库；十万条以上才需要考虑 ANN 索引（HNSW/faiss）
- 真正的最大杠杆是**上下文收窄**——如果能从项目/页面上下文知道用的是哪个设计系统/库，直接把搜索空间限定在该库内，问题退化回原来的规模
- 库一多，调用次数可能不变，但每次调用的候选 token 量会涨（同名/相似组件变多），所以"控制候选规模"和"减少调用次数"要一起考虑

### 方案 4：端到端单次 LLM 调用（反向思路）

不做任何检索，把压缩后的索引整体塞进一个长上下文模型的 prompt，靠一次调用让 LLM 自己完成"理解意图 + 选组件集 + 选变体"全流程。几乎不需要写新代码，只是把现在的多步调用合并成一次大请求。

## 2. 对比

| 维度 | 方案1 规则优先闸门 | 方案2 Embedding检索 | 方案3 规则+检索+路由 | 方案4 端到端单次调用 |
|---|---|---|---|---|
| **实现复杂度** | 低（加一个判断分支 + 一张配置表） | 中（建索引脚本、相似度计算、改 pipeline） | 高（索引重构、多级分支、维护归一化表和路由规则） | 最低（几乎只改 prompt） |
| **新增依赖数量** | 1 个（模糊匹配库，如 string-similarity） | 1~2 个（embedding 接入方式 + 相似度计算，可纯手写无需库） | 4~6 个（embedding、模糊匹配、分词、缓存、重排…） | 0 个 |
| **一次性开发成本** | 最低，几小时到一天 | 中，主要在建索引和调试相似度阈值 | 最高，需要较大重构和持续维护归一化表 | 最低，几乎是改改 prompt |
| **长期运行成本** | 仅对结构化输入有效，明显下降 | 下降约 1/3~1/2（省掉一次 LLM，换成低成本 embedding 调用/本地推理），对自然语言输入同样有效 | 最低（多数查询可做到 0~1 次 LLM） | **可能不降反升**——候选越多塞进 context 的 token 越多，费用随索引规模线性增长 |
| **适用规模** | 不限规模（只看输入是否结构化） | 单库到中等多库（几千到一两万条，内存算余弦相似度足够） | 大规模多库（几万条以上，需要分层和重排支撑精度） | 仅适合单库小规模；几万条索引塞不进上下文，扩展性最差 |
| **主要风险/局限** | 只对有结构化 `variant_props` 的输入生效，自然语言描述仍走老路径 | 需要选型并验证 embedding 模型在中文/专业术语上的效果，多了一次离线建索引的维护成本 | 改动面最大，需要持续维护归一化表和路由规则，调试周期长 | 库一多就不可行；为保持长 prompt 推理质量通常要用更贵的模型 |

## 3. 候选库清单（按模块）

| 模块 | 候选库 | 说明 |
|---|---|---|
| Embedding 生成 | 复用已有 `openai` SDK 指向 embedding 端点（DashScope/通义千问、OpenAI text-embedding-3-small）；或本地 `@xenova/transformers`（跑 bge-small-zh 等小模型 ONNX 版本） | 云端接入改动最小；本地推理省 API 费用 |
| 向量检索 | 几千到一两万条：手写余弦相似度即可，无需引入库；更大规模可选 `hnswlib-node`（HNSW ANN索引）或 `vectra`（本地向量索引） | 当前规模无需向量数据库 |
| 中文分词 | `nodejieba`（效果好，需原生编译）或 `segment`（纯 JS，跨平台更省心） | 增强关键词检索，作为 embedding 的补充/降级方案 |
| 模糊字符串匹配 | `fastest-levenshtein`、`string-similarity`、`fuse.js` | 用于变体属性值归一化兜底，处理"大"/"large"/"L"这类不一致 |
| 属性归一化表 | 不是库，是数据工程——JSON 配置文件，可从 `merged.json` 半自动统计生成初版再人工校对 | 跨库属性名/取值同义词映射 |
| 缓存 | `lru-cache`；语义缓存可直接复用 embedding 基础设施做相似度命中判断 | |
| LLM 调用 | 现有 `openai`、`@anthropic-ai/sdk` 保持不变 | 改动只是"什么时候调用"，调用方式（function calling/tool use）可直接复用 |

## 4. 推荐路径

1. **优先做方案 1**——改动最小、依赖最少（一个模糊匹配库 + 一张配置表），对结构化输入收益立竿见影，且不与后续方案冲突。但要清楚它不是万能药：如果实际查询大多是纯自然语言描述，整体收益有限。
2. **其次做方案 2**——对"主流量"（自然语言描述）更有效，改动量可控（离线建索引是一次性工作），运行成本继续下降。
3. **方案 3 留作多库规模上来之后的目标架构**——投入产出比在当前规模下不划算，等真正出现"几十个库、检索精度下降"问题时再分阶段补齐分层路由和重排。
4. **方案 4 不推荐**——看似最简单，但本质是把检索工作转嫁给模型长上下文，规模一大成本和延迟都会失控，属于"短期省事、长期埋雷"的选择。

## 5. 对照四个目标重新审视：token 低 / 准确性高 / 耗时短 / 整页风格一致

把所有讨论过的修改点放到这四个目标下重新检验一遍，会发现**它们彼此之间是有张力的，不能简单叠加**：

| | token 消耗 | 准确性 | 耗时 | 整页一致性 |
|---|---|---|---|---|
| 缩小候选数 / 跳过语义提取 | ↓ 有利 | 风险：候选少了可能漏召回 | ↑ 有利 | 不影响（单条场景） |
| 逐条缓存 / 逐条高置信度直通 | ↓ 有利 | 中性 | ↑ 有利 | **↓ 有害**——破坏集中裁决，见上节 |
| 把所有实例集中起来一次裁决 | 候选越多 prompt 越大，↑ 不利 | ↑ 有利（模型看到全局判断更准） | 单次调用变慢（prompt大）但总次数少 | ↑ 有利——这是一致性的根本来源 |

**核心矛盾**：要"省 token / 求快"最直接的办法是把实例摘出去单独处理（缓存、直通、并行），但"整页一致性"恰恰要求相关实例必须被同一次裁决看见——这两者天然对立。前面提出的"聚类 + 规范映射表 + 整簇决策 + 回灌对齐基准"安全网，本质就是在尝试同时满足这两边：**用聚类和映射表去掉真正不需要 LLM 看见的部分（重复/已确认的），把 LLM 的注意力集中在真正需要它做全局判断的"新的、有歧义的"那一小撮上**。

### 一个值得明确的认知调整：把"规范映射表"当成一致性的*增强*手段，而不是风险点

最初设想"缓存"是在牺牲一致性换速度；但如果把它从"临时加速缓存"重新定义成**"项目/设计系统级别的规范映射字典"**，它反而能提供一种**比"仅页面内一致"更强的一致性**——保证同一设计系统下，"确定按钮"无论出现在哪个页面，每次都解析到同一个组件+变体。这甚至比单纯依赖 LLM 集中裁决更可靠：LLM 本身存在非确定性，同一批次内能保证一致，但*跨批次/跨页面/跨次请求*之间未必一致（同样的输入两次跑可能因为模型采样而选出不同结果）。规范映射表恰好补上了这个缺口。

这样四个目标的关系就不再是"此消彼长"，而是：
- **首次遇到新簇** → 走集中裁决（保证当批次内一致、且利用模型的全局判断力保证准确性）→ 结果沉淀进映射表
- **再次遇到同簇**（同页或跨页）→ 直接查表 → token 几乎为零、耗时降到毫秒级、且比"重新裁决一次"在跨页场景下*更*一致

唯一需要新增的工程投入是维护这张表的"生命周期"：
- **失效机制**：组件库索引重建（[clearIndexCache, L80-82](nodejs/component-service/match_variant.js#L80-82)）后，映射表里指向旧 `componentKey`/`variantKey` 的条目要能被识别并清除或重新核验，否则会出现"表里查到的组件已经不存在了"
- **作用域问题**：同一个 label（比如"确定按钮"）在不同页面意图/上下文下，是否真的应该映射到同一个变体？如果答案是"通常应该"（多数设计系统确实要求同类元素全局统一），那么纯按 label 归一化建表是合理的；如果存在"页面 A 的确定按钮和页面 B 的不一样是设计上故意的"这种情况，就需要在映射表的 key 里加入更多上下文维度（比如 pageIntent），表会变复杂，命中率也会下降——这一点需要结合实际设计规范来定，不能想当然

### 结论

不要把"减少调用"当成单一目标去优化，否则很容易写出"更快但更不一致"或"更省但更不准"的方案。四个目标里，**"整页一致性"应该被当作硬约束（不可妥协），其余三个（token、准确性、耗时）是在满足这个约束的前提下去争取的**。"聚类 + 规范映射表"这条路径之所以值得优先投入，是因为它是少数几个能让四个目标同时变好（而不是相互牺牲）的杠杆——但它的收益会随映射表的成熟度逐渐显现，不是一上线就立竿见影，需要预期管理；并且它新增的工程投入主要在"维护表的生命周期"上，这部分容易被低估，需要提前规划。
